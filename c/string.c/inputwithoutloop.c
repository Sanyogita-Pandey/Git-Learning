#include<stdio.h>
#include<string.h>
int main(){
    char str[555];// can enter any no greater aur equal to the size
    gets(str);
    // scanf("%[^\n]s",str);
    // printf(" input is :%s",str);
    printf(" input is :%s",str);
    return 0;
}